<h1> Posts </h1>
<table>
  <tr>
    <td>Title</td>
    <td>Content</td>
  </tr>
  <tr>
    <% @posts.each do |post| %>
    <td><%= post.title %></td>
    <td> <%= post.content %> <%= link_to 'Edit', edit_post_path(post) %><%= link_to 'Show', post_path(post) %></td>
    <% end %>
  </tr>
</table>


